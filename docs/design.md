# 小狐大世界 - 项目策划文档

> 最后更新：2026-04-27

## 一、项目概述

- **项目名称**：小狐大世界（Fox Hollow Tales）
- **类型**：增量放置 / 村庄建设
- **灵感来源**：Kittens Game（猫国建设者），玩法机制相似，但所有文案、命名、主题完全原创
- **主题**：玩家经营一个狐狸山谷村落，从采集野莓开始，逐步建造设施、吸引村民、发展科技
- **风格**：白底黑字纯文字界面，紧凑单行排版
- **线上地址**：https://2d9lpj7e.mule.page/

---

## 二、核心机制

### 2.1 资源体系

| 分类 | 资源 | 初始上限 | 获取方式 |
|------|------|----------|----------|
| 基础 | 野莓 | 5000 | 手动采集 / 莓果园 / 采集者 |
| 基础 | 圆木 | 200 | 手动拾取 / 伐木场 / 伐木工 |
| 基础 | 碎石 | 200 | 手动捡拾 / 采石坑 / 矿工 |

| 加工 | 兽皮 | 50 | 鞣革坊 / 猎手 |
| 加工 | 木板 | 100（研究后解锁） | 工坊加工 |
| 加工 | 砖块 | 100（研究后解锁） | 工坊加工 |
| 加工 | 矿铁 | 50 | 锻造炉 / 铁匠 |
| 知识 | 卷轴 | 25 | 工坊加工 / 学者被动产出 |
| 知识 | 学识 | 50 | 藏书阁 / 学者 |
| 贸易 | 铜钱 | 50 | 集市摊 / 商贩 |
| 神秘 | 符咒 | 25 | 灵狐祠 |
| 神秘 | 遗光 | 无上限 | 极低概率被动获得 / 手动采集触发 / 山谷见闻奖励 |

### 2.2 建筑系统

所有建筑价格增长系数统一为 **1.12**。建筑可出售，返还当前等级造价的 50%。

| 建筑 | 基础费用 | 效果 | 解锁条件 |
|------|----------|------|----------|
| 莓果园 | 野莓8 | 野莓+0.15/s | 初始 |
| 狐狸窝 | 野莓70, 圆木4 | 容量+2 | 初始 |
| 伐木场 | 野莓60, 圆木8 | 圆木+0.03/s | 初始 |
| 采石坑 | 野莓80, 圆木10 | 碎石+0.02/s | 初始 |

| 鞣革坊 | 圆木20, 野莓120 | 兽皮+0.0075/s | 伐木场×1 |
| 储藏窖 | 圆木18, 碎石8 | 多项上限提升 | 采石坑×1 || 藏书阁 | 圆木30, 碎石15 | 学识上限+50, 学识+0.015/s | 储藏窖×1 |
| 锻造炉 | 圆木40, 碎石25 | 矿铁上限+50, 矿铁+0.004/s | 藏书阁×1 |
| 集市摊 | 圆木50, 碎石20, 兽皮5 | 铜钱上限+50, 铜钱+0.002/s | 鞣革坊×1 |
| 灵狐祠 | 碎石40, 矿铁3, 学识15 | 符咒上限+25, 符咒+0.006/s, 满意度+5% | 藏书阁×1, 锻造炉×1 |
| 月光井 | 碎石50, 符咒6 | 全资源产量+3% | 灵狐祠×1 |

| 板屋 | 木板5, 砖块3, 野莓200 | 容量+3 | 研究：木工技艺 + 石砌术 |
| 石窖 | 木板3, 砖块5, 矿铁3 | 多项资源上限大幅提升 | 研究：铁器冶炼 |
| 商驿 | 铜钱15, 砖块4, 兽皮3 | 全资源产量+2%, 铜钱上限+30 | 集市摊×2 |

### 2.3 职业系统

狐狸村民可分配到不同职业。每个职业可通过「授业」消耗卷轴提升等级，每级产出+10%。

| 职业 | 产出 | 解锁条件 |
|------|------|----------|
| 采集者 | 野莓+0.5/s | 初始 |
| 伐木工 | 圆木+0.06/s | 伐木场×1 |
| 矿工 | 碎石+0.04/s | 采石坑×1 |

| 猎手 | 兽皮+0.015/s | 鞣革坊×1 |
| 学者 | 学识+0.04/s, 卷轴+0.0025/s, 符咒+0.001/s | 藏书阁×1 |
| 铁匠 | 矿铁+0.01/s | 锻造炉×1 |
| 商贩 | 铜钱+0.005/s | 集市摊×1 |

**授业系统**：消耗卷轴为职业升级，费用 = 当前等级 + 2 卷轴，每级该职业所有从业者产出+10%。

### 2.4 研究系统

| 研究 | 费用 | 效果 | 解锁条件 |
|------|------|------|----------|
| 石制工具 | 学识8, 碎石15 | 野莓产量+50%, 圆木+30% | 藏书阁×1 |

| 木工技艺 | 学识20, 圆木30 | 解锁木板, 圆木+30% | 藏书阁×1, 伐木场×1 |
| 石砌术 | 学识25, 碎石35 | 解锁砖块, 碎石+30% | 藏书阁×1, 采石坑×1 |
| 林间密语 | 学识35, 圆木20 | 圆木+50% | 石制工具完成 |
| 铁器冶炼 | 学识40, 矿铁10 | 矿铁+50% | 锻造炉×1 |
| 狐灵传说 | 学识50, 符咒3 | 符咒+50%, 满意度+10% | 灵狐祠×1 |
| 灵狐庇护 | 学识45, 符咒8, 卷轴3 | 冬季野莓倍率 ×0.25→×0.4 | 狐灵传说完成 |
| 先祖之眼 | 学识35, 符咒5, 卷轴2 | 狐狸消耗野莓 -15% | 狐灵传说完成 |
| 工法精要 | 学识30, 木板3, 砖块2 | 解锁工坊自动制作 | 木工技艺 + 石砌术完成 |

### 2.5 工坊系统

| 配方 | 消耗 | 产出 | 解锁条件 |
|------|------|------|----------|
| 加工木板 | 圆木30 | 木板1 | 研究：木工技艺 |
| 烧制砖块 | 碎石30, 圆木5 | 砖块1 | 研究：石砌术 |
| 抄录卷轴 | 学识20, 兽皮1 | 卷轴1 | 藏书阁×1 |

### 2.6 灵术系统

需灵狐祠解锁，显示在营火 Tab 底部。

| 灵术 | 消耗 | 效果 | 限制 |
|------|------|------|------|
| 祈雨 | 符咒5 | 本季野莓产量×1.5 | 每季限一次 |
| 祖灵 | 符咒8 | 本季所有职业产出 +50% | 每季限一次 |
| 丰收祭 | 符咒3 | 本季建筑造价 -30%，触发一次山谷见闻 | 每季限一次 |

### 2.7 季节系统

每年4个季节循环，每季 100 天（游戏内时间）。

| 季节 | 野莓产量倍率 | 特殊效果 |
|------|-------------|----------|
| 早春 | ×1.5 | 丰收季 |
| 盛夏 | ×1.0 | 正常 |
| 金秋 | ×1.2 | 略丰 |
| 寒冬 | ×0.25（庇护后×0.4） | 产量骤降，需提前囤粮 |

### 2.8 满意度系统

- 基础值：100%
- 人口 > 5 时，每多1只狐狸 -2%
- 灵狐祠每座 +5%
- 研究「狐灵传说」+10%
- 范围：10% ~ 200%
- 影响手动采集野莓的产量
- 影响全体职业产出（乘以满意度百分比）
- 悬浮面板显示计算明细及当前职业产出倍率

### 2.9 人口系统

- 狐狸通过「狐狸窝」提供容量（每座+2）
- 当容量有余且野莓 > 20 时，有 1.5% 概率每 tick 迁入新狐狸
- 研究「先祖之眼」后狐狸消耗野莓 -15%（0.35→0.30/s）
- 野莓耗尽时，每 50 tick 有一只狐狸离开
- 每只狐狸消耗野莓 0.35/s

### 2.10 事件系统

#### 山谷见闻
- 25 种随机事件，约每 10 分钟触发一次（概率 0.0003/tick）
- 四大类：灵启（需灵狐祠）、天象、山谷异象、过客与声音
- 加权随机选取，部分有季节/建筑前置条件
- 部分事件附带资源奖励

#### 世界的回响
- 18 段极稀有叙事文本，约每 3 小时触发一次（概率 0.00002/tick）
- 按季节分类（春4/夏5/秋5/冬4），仅当前季节可触发
- 斜体蓝灰色显示，与普通日志区分
- 纯叙事，无资源效果

### 2.11 存档系统

- **自动存档**：localStorage，每 30 秒自动保存
- **手动存档**：Header「存档」按钮
- **存档码**：Base64 编码导出/导入，支持跨设备迁移
- **后台补算**：切换标签页再切回时，自动补算离开期间的资源产出（含季节更替、狐狸消耗、饥荒、迁入）
- 最大补算时间 24 小时

---

## 三、UI 设计

- **风格**：白底黑字，纯文本，无图标无装饰
- **布局**：三栏（左：资源 | 中：操作 | 右：日志）
- **建筑行**：单行紧凑「名称 (数量) 费用 [建造] [出售]」
- **费用不足**：红色高亮
- **Tab**：营火 / 村落 / 工坊 / 研究

### 3.1 悬浮信息面板

所有游戏元素名称支持鼠标悬停显示信息面板（`position: fixed`，JS 定位防溢出）：
- 建筑：描述 + 效果数值 + 解锁提示 + 短句
- 职业：描述 + 每人产出 + 短句
- 研究：描述 + 加成百分比 + 短句
- 工坊：描述 + 短句
- 资源：短句 + 特殊提示
- 满意度：显示计算明细

### 3.2 短句系统

- 数据中以 `tip` 数组存储，支持多句
- 每季缓存一次，季节更替时刷新
- 风格：冷幽默、有哲思、克制、口语化

---

## 四、技术实现

- **纯前端**：HTML + CSS + JS，无框架
- **文件结构**：
  ```
  fox-hollow/
  ├── .git/               # Git 版本控制
  ├── index.html          # 入口页面
  ├── README.md           # 项目说明
  ├── css/
  │   └── style.css       # 样式
  ├── js/
  │   ├── data.js         # 数据定义（资源/建筑/职业/研究/工坊/灵术/事件）
  │   ├── engine.js       # 游戏引擎（状态/计算/tick/存档/事件触发）
  │   └── ui.js           # 界面渲染（日志/面板/Tab/悬浮面板）
  ├── docs/
  │   ├── RULES.md        # 协作规则
  │   ├── design.md       # 本策划文档
  │   └── changelog.md    # 变更日志
  └── reference/          # 猫国建设者（Kittens Game）中文 Wiki 参考资料
      ├── _navigation.md  # Wiki 导航目录
      ├── 首页.md          # Wiki 首页
      ├── 000-猫国萌新指导/  # 新手指导（萌新入门、政策解析、中期指导等）
      ├── 001-猫咪百科/      # 游戏百科（建筑/村庄/科学/工坊/宗教/空间/时间等）
      │   ├── 01-建筑物/    # 食物生产、猫口、科技、存储、资源、工业、文化、超级建筑等
      │   ├── 02-村庄/      # 职业、管理、猫口普查
      │   ├── 03-科学/      # 科学、玄学、政策
      │   ├── 04-工坊/      # 升级、工艺、工程师、斑马升级
      │   ├── 05-贸易.md    # 贸易系统
      │   ├── 06-宗教/      # 庙塔、太阳教团、奥秘神学、契约
      │   ├── 07-空间/      # 地面控制至边界环（14 个区域）
      │   └── 08-时间/      # 时间、重置、时间锻造、虚空领域
      ├── 002-常用资料/      # 游戏机制、价格指数、能源、数学技巧等
      ├── 003-资源大全/      # 60+ 种资源详解（猫薄荷至 T 秘银）
      ├── 004-第三方工具/    # 小猫科学家、Nummon 等辅助工具
      ├── 005-名词解释/      # 幸福度、黑暗未来、时间悖论、收益递减、天网觉醒
      └── 007-常见问题/      # FAQ、云存档、建筑升级、次元超越、推荐玄学等
  ```
- **存档**：localStorage + Base64 存档码
- **Tick**：200ms 间隔，每 5 tick 刷新界面
- **发布**：Mule Pages 静态站点

---

## 五、待开发功能（Backlog）

- [ ] 遗光用途：为遗光设计后期消耗场景（目前纯装饰性收集）
- [ ] 科技树可视化
- [ ] 成就系统
- [ ] 重置（轮回）机制：重置后获得永久加成
- [ ] 更多职业与专精
- [ ] 外交系统：与其他狐狸部落互动
- [ ] 手机端适配优化

---

## 六、已完成版本回顾

### v0.9.0 - 高级建筑（已完成）

解决了木板、砖块、矿铁、铜钱四种资源无消耗场景的问题，形成中期资源进阶循环。

- **板屋**：木板5 + 砖块3 + 野莓200，每座容纳 3 只狐狸，需木工技艺 + 石砌术
- **石窖**：木板3 + 砖块5 + 矿铁3，大幅提升多项资源上限，需铁器冶炼
- **商驿**：铜钱15 + 砖块4 + 兽皮3，全资源产量 +2% + 铜钱上限 +30，需集市摊 ≥ 2

### v0.9.1 - 遗光（已完成）

- 新增神秘资源「遗光」，无上限，极低概率被动获得/手动采集触发/山谷见闻奖励
- 纯装饰性体验，后期用途留待未来版本

### v0.10.0 - 工坊自动化（已完成）

- 新研究「工法精要」解锁后，工坊配方可开启自动制作
- 每 50 tick 自动执行，含保底线和产出上限检查

### v0.10.1 - 显示修正（已完成）

- 速率精度优化、浮点过滤、自动制作速率显示、后台补算阈值调整

### v0.11.0 - 远行与贸易（已完成）

全新「山外」Tab，包含远行系统、商队系统、叙事碎片收集。详见第八章。

- 3 种新贸易资源（香料、丝帛、古币）、2 座新建筑（驿道、瞭望塔）、1 种新职业（斥候）、2 项新研究
- 4 个远行目的地（旧墟30天 → 云岭60天 → 荒丘100天 → 密林150天）
- 旧墟 10 段 + 云岭 8 段叙事碎片，按远行次数顺序解锁
- 4 种外来商队周期到访交易
- 3 种新灵术（灵路、商风、山谷宴席）
- 5 个远行抉择事件（斥候 ≥ 2 时触发）
- 离线补算覆盖远行倒计时、商队到期、叙事暂存

### v0.11.1 - 远行 Bug 修复与体验优化（已完成）

- 斥候数量选择改为按钮组、自动制作连续速率模型优化、速率显示与暂停原因修正

### v0.11.2 - 远行目的地调序与文案（已完成）

- 旧墟/云岭（有叙事）优先解锁，荒丘/密林推后；近丘改名荒丘；旧墟/云岭添加描述与短句

---

## 七、当前版本状态

**最新版本**：v0.11.2（2026-04-27）

已实现的完整系统：
- 15 种资源（含遗光、香料、丝帛、古币）、16 种建筑（含驿道、瞭望塔）、8 种职业（含斥候）、11 项研究、3 个工坊配方
- 6 种灵术（祈雨、祖灵、丰收祭、灵路、商风、山谷宴席）
- 25 种山谷见闻、18 段世界回响、5 个远行抉择事件
- 4 个远行目的地（旧墟、云岭、荒丘、密林），旧墟 10 段 + 云岭 8 段叙事碎片
- 4 种外来商队（山猫行商、河獭商队、白鹤信使、旧墟遗民）
- 季节系统、满意度系统、人口系统、授业系统
- 远行系统、商队系统、抉择事件系统、山外 Tab
- 工坊自动化、建筑出售、存档码、离线补算（含远行/商队补算）
- 悬浮信息面板、短句缓存系统

---

## 八、远行与贸易（v0.11.0 - v0.11.2，已完成）

### 8.1 设计理念

猫国建设者的核心乐趣是计算最优路径；小狐大世界的差异化在于**叙事驱动的好奇心**。远行系统的核心不是"去换资源"，而是"去听故事"——每次远行返回带来叙事碎片，逐步拼出更大的世界观。

### 8.2 新增 Tab

- **「山外」Tab**：解锁条件为研究「山外见闻」完成
- 包含：远行面板、商队面板、叙事碎片收集

### 8.3 新增资源

| 资源 | 分类 | 初始上限 | 获取途径 |
|------|------|----------|----------|
| 香料 | 贸易 | 0 | 远行荒丘、山猫行商 |
| 丝帛 | 贸易 | 0 | 远行密林、河獭商队 |
| 古币 | 贸易 | 0 | 远行旧墟/云岭、旧墟遗民 |

上限由驿道和瞭望塔提供：

| 建筑 | 香料上限 | 丝帛上限 | 古币上限 |
|------|----------|----------|----------|
| 驿道每座 | +5 | +3 | +3 |
| 瞭望塔每座 | +3 | +5 | +3 |

首次获得时自动解锁显示（`on = true`）。

### 8.4 新增建筑

| 建筑 | 费用 | 效果 | 解锁条件 |
|------|------|------|----------|
| 驿道 | 木板3, 砖块5, 碎石30（×1.12） | 解锁远行 +1 并行队伍；香料上限+5, 丝帛上限+3, 古币上限+3 | 研究：山外见闻 |
| 瞭望塔 | 砖块8, 矿铁5, 圆木20（×1.12） | 远行时间 -15%/座（乘法叠加）；香料上限+3, 丝帛上限+5, 古币上限+3；被动消耗符咒 | 驿道×1 |

**瞭望塔符咒维持**：引擎 tick 速率 `charmP: -0.01`，显示 -0.005/s，实际消耗 **1.0 符咒/季**。1 座灵狐祠产出 1.2/季，可供养 1 座瞭望塔并略有盈余。

**瞭望塔失效机制**：新增辅助函数 `activeWatchtowers()`，返回当前有效的瞭望塔数量。判定逻辑：若 `G.res.charm.v <= 0` 且瞭望塔的 charmP 消耗使净符咒产量为负，则返回 0（全部失效）；否则返回 `G.bld.watchtower.c`。所有使用瞭望塔效果的地方（远行时间计算、远行面板显示）统一调用此函数，而非直接读 `G.bld.watchtower.c`。瞭望塔的资源上限效果（spiceMx/silkMx/ancCoinMx）不受失效影响，仅远行时间减免失效。不中断进行中的远行。

### 8.5 新增职业

| 职业 | 效果 | 解锁条件 |
|------|------|----------|
| 斥候 | 无被动产出；每个斥候使远行奖励数量 +20%（加法叠加）；斥候 ≥ 2 时远行有概率触发抉择事件 | 驿道×1 |

**斥候显示**：斥候无 `*P` 产出键，`jobEffects()` 对斥候返回特殊文本"远行奖励 +20%/人"。JD 定义中 `e: {}`（空对象），另加 `desc: '远行奖励 +20%/人'` 字段供 UI 读取。

### 8.6 新增研究

| 研究 | 费用 | 效果 | 解锁条件 |
|------|------|------|----------|
| 山外见闻 | 学识30, 铜钱10, 卷轴2 | 解锁「山外」Tab + 驿道建筑 | 集市摊×1, 藏书阁×1 |
| 远途跋涉 | 学识45, 丝帛2, 古币1 | 远行奖励 +50%，解锁云岭目的地 | 驿道×2, 远行旧墟完成≥1次 |

### 8.7 远行系统

#### 操作流程

1. 在「山外」Tab 选择目的地
2. 消耗资源（野莓干粮 + 铜钱路费 + 符咒灵力）
3. 派出 1-3 只闲置狐狸（锁定为外出状态）
4. 等待游戏内天数倒计时
5. 返回：获得资源奖励 + 触发叙事（占位，待审核）

#### 远行记录结构

`G.expeditions` 数组，每条记录：

```
{
  dest: string,          // 目的地 ID（nearHill / forest / oldRuin / cloudRidge）
  foxCount: number,      // 派出狐狸数
  ticksLeft: number,     // 剩余 tick 数
  totalTicks: number,    // 总 tick 数（用于进度条计算）
  usedSpiritPath: boolean // 是否已对此次远行施放过灵路
}
```

#### 目的地

| 目的地 | 路程 | 消耗 | 解锁条件 |
|--------|------|------|----------|
| 旧墟 | 30天 | 野莓50, 铜钱2 | 驿道×1 |
| 云岭 | 60天 | 野莓100, 铜钱5, 符咒1 | 驿道×1, 瞭望塔×1, 研究：远途跋涉 |
| 荒丘 | 100天 | 野莓200, 铜钱8, 符咒4 | 驿道×3, 瞭望塔×1 |
| 密林 | 150天 | 野莓300, 铜钱10, 符咒8 | 驿道×3, 灵狐祠×2 |

路程受瞭望塔缩减：每座 ×0.85（2 座 = ×0.72，3 座 = ×0.61）。

#### 奖励池

每次返回从目的地奖励池抽取 2-3 项，数量 = 基础数量 × (1 + 斥候数 × 0.2) × (研究「远途跋涉」? 1.5 : 1)。

| 目的地 | 奖励池（基础数量） |
|--------|-------------------|
| 旧墟 | 矿铁 3-6, 古币 1-2, 遗光 1（10%概率），叙事碎片（必定） |
| 云岭 | 符咒 3-5, 古币 2-3, 叙事碎片（必定） |
| 荒丘 | 碎石 10-20, 兽皮 1-3, 香料 1（40%概率） |
| 密林 | 圆木 15-25, 兽皮 2-4, 丝帛 1（40%概率） |

旧墟和云岭每次必定返回一段叙事碎片（按顺序推进，不重复抽取）。

#### 抉择事件

斥候 ≥ 2 时，远行返回有概率触发抉择事件。玩家从 2 个选项中选择，不同选项导致不同后果（资源变化、新事件加入事件池、特殊状态等）。无最优解。

抉择事件内容全部为占位符，待审核后填入。

#### 叙事碎片收集状态

已收集的叙事碎片以 `G.narratives` 持久化：

```
G.narratives = {
  oldRuin: [],      // 已解锁的旧墟碎片索引数组，如 [0, 1, 2]
  cloudRidge: []    // 已解锁的云岭碎片索引数组
}
```

远行返回时，取 `G.narratives[dest].length` 作为下一条碎片的索引，若未超出数据总条数则推入。山外 Tab"叙事碎片"区域按序展示已收集条目，未收集的显示为"???"占位。

#### 外出狐狸状态

- 新增 `G.foxAway` 追踪外出狐狸数
- 派出时：`G.foxAway += n`，`G.freeFox -= n`
- 返回时：`G.foxAway -= n`，`G.freeFox += n`
- 外出狐狸**不消耗村内野莓**（出发时已扣干粮）
- 外出狐狸**不受饥荒减员影响**
- 野莓消耗计算用 `G.foxes - G.foxAway`
- `rmFox()` 只从村内狐狸（含职业和闲置）中移除

### 8.8 商队系统

外来商队为其他物种的狐族或山谷中的灵兽，周期性到访。

#### 机制

- 每 2-3 个季节随机有商队到访，停留 1 个季节后离开
- 到访时日志提示（占位），「山外」Tab 显示商队商品
- 每种商品每次驻留可购买 1 次
- 冷却追踪：`G.caravanTimer`（整数），每次季节更替 +1；商队离开时重置为 0；当 `caravanTimer >= 2` 时每季有 50% 概率到访（保证间隔 2-3 季）；商队在场时不计数

#### 商队类型

| 商队 | 商品 A | 商品 B | 收购（玩家卖出换铜钱） | 出现条件 |
|------|--------|--------|----------------------|----------|
| 山猫行商 | 香料×2 = 铜钱8 | 兽皮×3 = 铜钱5 | 圆木30 → 铜钱3 | 默认 |
| 河獭商队 | 丝帛×2 = 铜钱10 | 矿铁×3 = 铜钱8 | 木板2 → 铜钱5 | 瞭望塔×1 |
| 白鹤信使 | 卷轴×2 = 铜钱12 | 符咒×2 = 铜钱10 | 砖块3 → 铜钱5 | 灵狐祠×1 |
| 旧墟遗民 | 古币×2 = 铜钱15 | 符咒×3 = 铜钱12 | 不收购 | 远行旧墟完成≥1次 |

商队到访/离开日志为占位符，待审核后填入。

### 8.9 灵术扩展

新增 2 个灵术 + 1 个宴席机制：

| 灵术/机制 | 消耗 | 效果 | 限制 |
|-----------|------|------|------|
| 灵路 | 符咒6 | 当前进行中的一次远行剩余时间 -30% | 每次远行限用一次 |
| 商风 | 符咒10 | 立即召来一支随机商队（停留1季） | 每年限一次；已有商队在场时不可施放 |
| 山谷宴席 | 香料2, 野莓100 | 满意度 +15%，持续本季 | 每季限一次；需驿道×1 |

灵路在叙事框架中的含义：灵力引导，此次远行有概率触发更深层叙事。

**灵路目标选择**：若仅 1 支队伍在外，自动对其施放；若多支队伍同时在外，默认对剩余时间最长（ticksLeft 最大）的队伍施放。按钮显示目标目的地名，如"灵路（→旧墟）"。若所有在外队伍均已使用过灵路，按钮置灰。

商风状态追踪：`G.tradeWindYear`，当年施放过则不可再施放。已有商队时按钮置灰显示"（商队在场）"。

宴席状态追踪：`G.feastSeason`，当季施放过则不可再施放。`calcH()` 中 `if (G.feastSeason === G.season) h += 0.15`。

### 8.10 符咒消耗场景汇总

| 消耗来源 | 类型 | 大约消耗量 |
|----------|------|-----------|
| 灵术（祈雨/祖灵/丰收祭） | 每季主动 | 3-16/季 |
| 远行出发（密林/旧墟/云岭） | 每次主动 | 1-8/次 |
| 灵路加速 | 每次远行可选 | 6/次 |
| 商风召唤 | 每年可选 | 10/年 |
| 瞭望塔维持 | 持续被动 | 1.0/座/季 |
| 月光井建造 | 一次性 | 6×1.12^n |

### 8.11 离线补算

`simulateOffline()` 新增：

- **远行倒计时推进**：每 tick 减少剩余天数，到期自动结算奖励（静默，不输出叙事）
- **返回提示**：回到游戏时日志显示"离开期间，远行队伍从 [目的地] 返回了。"（占位）
- **叙事暂存**：离线期间完成的远行，叙事碎片存入 `G.pendingNarr` 队列，回到游戏后逐条展示
- **商队到期**：离线期间商队停留时间耗尽则自动离开
- **瞭望塔维持**：离线期间符咒持续扣除，不足时瞭望塔标记失效

### 8.12 解锁检查扩展

`chk()` 新增条件类型 `q.exp`，检查远行完成历史：

- 状态追踪：`G.expDone = { nearHill: 0, forest: 0, oldRuin: 0, cloudRidge: 0 }`（完成次数）
- 示例：研究「远途跋涉」的 `uq: { b: { trailroad: 2 }, exp: { oldRuin: 1 } }`

### 8.13 存档兼容

`migrate()` 补齐旧存档：

```
G.expDone = G.expDone || {}
G.expeditions = G.expeditions || []
G.foxAway = G.foxAway || 0
G.pendingNarr = G.pendingNarr || []
G.narratives = G.narratives || { oldRuin: [], cloudRidge: [] }
G.feastSeason = G.feastSeason ?? -1
G.tradeWindYear = G.tradeWindYear ?? -1
G.caravan = G.caravan || null
G.caravanTimer = G.caravanTimer || 0
G.pendingChoice = G.pendingChoice || null
G.choicesDone = G.choicesDone || []
G.choiceBuffs = G.choiceBuffs || {}
```

新增资源/建筑/职业/研究通过现有 `migrate()` 末尾的补齐循环自动处理。

### 8.14 涉及文件改动

| 文件 | 改动 |
|------|------|
| `data.js` | 新增 3 种资源 RD；2 座建筑 BD（驿道、瞭望塔）；1 种职业 JD（斥候）；2 项研究 UD；3 个灵术 SD（灵路、商风、山谷宴席）；远行目的地数据 EXD（含每目的地 6 条返回日志）；叙事碎片 NARR（旧墟 10 段、云岭 8 段）；商队数据 CVD（4 种商队含到访/离开日志）；抉择事件 CHOICE_EVENTS（5 个）；TABS 增加山外 |
| `engine.js` | `G` 新增 foxAway/expDone/expeditions/pendingNarr/narratives/feastSeason/tradeWindYear/caravan/caravanTimer/pendingChoice/choicesDone/choiceBuffs 等状态；新增 `activeWatchtowers()`/`expTimeMul()` 辅助函数；`calcR()` 野莓消耗用 foxes-foxAway；`calcMx()` 增加驿道/瞭望塔的新资源上限；`calcH()` 增加宴席/抉择掌印墙加成；`chk()` 增加 exp 条件类型；`tick()` 增加远行倒计时/商队到期/商队随机到访（含 caravanTimer 逻辑）；新增 sendExpedition()/resolveExpedition()/buyFromCaravan()/sellToCaravan()/castSpell 扩展灵路商风宴席；新增 tryTriggerChoice()/applyChoice() 抉择事件系统（含 5 种事件的具体效果实现）；`rmFox()` 排除外出狐狸；`simulateOffline()` 覆盖远行/商队；`migrate()` 补齐新状态 |
| `ui.js` | `rTC()` 新增山外 Tab 渲染（远行面板含进度条 + 商队面板 + 叙事碎片列表）；营火 Tab 灵术区新增灵路/商风/宴席；新增 showChoiceModal() 抉择事件弹窗；斥候数量选择改为按钮组 |
| `style.css` | 山外 Tab 相关样式（远行进度条、商队商品行、叙事碎片区域） |
| `docs/design.md` | 本章节 |
| `docs/changelog.md` | 追加 v0.11.0 ~ v0.11.2 |

### 8.15 叙事内容清单（已完成）

| 类型 | 数量 | 说明 |
|------|------|------|
| 远行返回日志 | 每目的地 6 条 | 通用返回叙事，随机抽取 |
| 旧墟叙事碎片 | 10 条 | 按顺序推进，拼出旧墟故事线（从陶片发现到南方新城） |
| 云岭叙事碎片 | 8 条 | 按顺序推进，拼出云岭故事线（从掌印到跨代际望孔） |
| 抉择事件 | 5 个 | 每个含 2 选项 + 各自后果，有资源/buff 实际效果 |
| 商队到访/离开日志 | 每种商队各 1 条 | 到访和离开各一条，带有物种特征 |
| 新建筑/职业/研究/灵术/目的地 tip | 各数条 | 短句风格同现有 |

---

## 九、图纸与专精系统（v0.12.0，设计中）

### 9.1 设计理念

增量游戏中后期最大的问题是"堆数量无决策"——建筑造到 10 座和造到 20 座之间只有数值差异，没有策略差异。图纸与专精系统通过**不可撤销的方向选择**为每座建筑和每种职业引入分岔路径，让不同玩家的村庄产生实质性的结构差异。

获取方式绑定商队的随机到访和概率携带，保持放置游戏"等待+抓机会"的节奏——图纸来了就看看要不要买，不来就继续挂机。

### 9.2 图纸

图纸是一种**具名物品**，不是通用资源。每张图纸对应一个建筑或职业的某个专精方向。

- 存储：`G.blueprints` 数组，元素为 `{ id: 'berryPatch_A', target: 'berryPatch', spec: 'A' }`
- 图纸不可堆叠，同一种图纸只能持有一张（已拥有则商队不会再带）
- 同一目标的 A/B 图纸互斥持有：若已持有 berryPatch_A，则 berryPatch_B 不会出现在商队中，反之亦然
- 已激活的专精对应图纸从背包移除；若激活 A 时背包中持有同目标的 B，B 自动丢弃
- 在「工坊」Tab 新增「图纸」区域展示已持有的图纸，点击可查看效果并激活

### 9.3 商队携带图纸

商队到访时有 **50% 概率**额外携带一张图纸。图纸种类从该商队的图纸池中随机抽取（排除玩家已拥有、已激活、以及同目标另一方向已持有的）。**图纸仅在玩家拥有对应资源上限时才会生成**（例如香料上限 > 0 才会出现需要香料的图纸）。

| 商队 | 图纸池 |
|------|--------|
| 山猫行商 | 莓果园 / 伐木场 / 采石坑专精、采集者天赋 |
| 河獭商队 | 鞣革坊 / 锻造炉 / 储藏窖专精、伐木工 / 矿工天赋 |
| 白鹤信使 | 藏书阁 / 灵狐祠专精、学者 / 铁匠天赋 |
| 旧墟遗民 | 猎手 / 商贩天赋 |

图纸价格 = 铜钱底价 + 该商队对应的稀有资源：

| 商队 | 图纸价格 |
|------|----------|
| 山猫行商 | 铜钱 12 + 香料 1 |
| 河獭商队 | 铜钱 15 + 丝帛 1 |
| 白鹤信使 | 铜钱 18 + 古币 1 |
| 旧墟遗民 | 铜钱 20 + 古币 2 |

### 9.4 建筑专精

以下 8 种生产型建筑提供专精选项；狐狸窝、集市摊、月光井、板屋、石窖、商驿、驿道、瞭望塔不设专精。

每种建筑达到 **5 座**后可激活专精。激活需消耗对应图纸，**每种建筑只能选一个方向，不可撤销**。

A/B 两个方向对应同一建筑的不同图纸，玩家手里拿到哪张就只能选哪个方向——除非等到下次商队带来另一张。也可以攒着不激活，等拿到心仪的那张再用。

| 建筑 | 专精·甲 | 效果 | 专精·乙 | 效果 |
|------|---------|------|---------|------|
| 莓果园 | 沃土 | 产量 +40%，造价 +25% | 野蔓 | 造价 -20%，产量 +10% |
| 伐木场 | 深林 | 圆木 +50%，每座消耗碎石 0.005/s | 杂伐 | 圆木 +15%，额外产兽皮 0.002/s |
| 采石坑 | 掘脉 | 碎石 +50%，每座消耗圆木 0.005/s | 拾遗 | 碎石 +20%，额外产矿铁 0.001/s |
| 鞣革坊 | 厚韧 | 兽皮 +60%，全村野莓消耗 +10% | 薄削 | 兽皮 +20%，兽皮 -0.01/s 转化为铜钱 +0.005/s（等效持续转化） |
| 锻造炉 | 烈焰 | 矿铁 +60%，每座消耗圆木 0.01/s | 巧工 | 矿铁 +25%，所有建筑矿铁造价 -15% |
| 储藏窖 | 深窖 | 上限效果翻倍 | 霜藏 | 野莓上限 +50%，寒冬野莓产量倍率提升 10%（0.25 → 0.275，若已有灵狐庇护则 0.4 → 0.44） |
| 藏书阁 | 穷卷 | 学识 +50%，卷轴产出 +30% | 秘阁 | 学识上限翻倍，学识 +15% |
| 灵狐祠 | 福佑 | 满意度额外 +3%/座 | 引灵 | 符咒 +40%，灵术消耗 -20% |

### 9.5 职业天赋

激活方式与建筑专精相同：消耗对应图纸，全局生效于该职业所有从业者，**不可撤销**。无数量门槛，拿到图纸即可激活。

斥候不设天赋——其定位为远行辅助职业，已有"远行奖励 +20%/人"的内在成长曲线，无需额外分岔。

| 职业 | 天赋·甲 | 效果 | 天赋·乙 | 效果 |
|------|---------|------|---------|------|
| 采集者 | 勤爪 | 产量 +30%，每人多消耗野莓 0.05/s | 轻手 | 产量 +15%，手动采集量 +50% |
| 伐木工 | 老斧 | 产量 +40% | 兼顾 | 产量 +15%，额外产碎石 0.01/s |
| 矿工 | 稳镐 | 产量 +40% | 探脉 | 产量 +15%，额外产矿铁 0.002/s |
| 猎手 | 循迹 | 兽皮 +50% | 哨眼 | 兽皮 +20%，远行奖励额外 +10%/猎手（与斥候 +20%/人加法叠加，见 8.7 奖励倍率公式） |
| 学者 | 沉思 | 学识 +40%，卷轴 +30% | 通览 | 学识 +20%，研究费用 -10% |
| 铁匠 | 锤淬 | 矿铁 +50% | 省料 | 矿铁 +20%，锻造炉造价 -15% |
| 商贩 | 精算 | 铜钱 +50% | 广路 | 铜钱 +20%，商队图纸出现概率提升（基础 50% × 1.2 = 60%） |

### 9.6 引擎实现要点

#### 专精效果生效

`calcR()` 中在建筑产出和职业产出阶段检查 `G.bldSpec[id]` / `G.jobTalent[id]`，应用对应的乘数或附加产出。专精效果以**乘法叠加**应用在基础产出上，不与研究加成冲突。

示例：莓果园「沃土」激活后，`berryP` 产出从 `0.15/s × 座数` 变为 `0.15 × 1.4 × 座数`。

#### 造价修正

锻造炉「巧工」、铁匠「省料」等造价减免效果通过新函数 `specCostMul(id, resKey)` 返回造价乘数，`bp()` 中调用。多个造价修正以**乘法叠加**，如巧工 ×0.85 × 丰收祭 ×0.7 = ×0.595。

#### 消耗类专精

鞣革坊「厚韧」的"全村野莓消耗 +10%"仅作用于 `foxEatRate()` 返回的基础狐狸食量，**不**放大采集者「勤爪」的额外 0.05/s 消耗。两者各自以独立加项叠加在 `r.berry` 上：先扣 foxEat × 1.1 × 村内狐狸数，再扣 0.05 × 采集者数。

伐木场「深林」每座消耗碎石 0.005/s，在 `calcR()` 中作为负产出叠加：
```
if (G.bldSpec.lumberYard === 'A') r.stone -= 0.005 * G.bld.lumberYard.c * 5;
// ×5 因为 tick 间隔 200ms，0.005/s = 0.001/tick × 5
```

#### 薄削连续转化

鞣革坊「薄削」以连续速率建模，在 `calcR()` 中叠加负兽皮产出和正铜钱产出（仅当兽皮 > 0 且铜钱未满时生效）：
```
if (G.bldSpec.tannery === 'B' && G.res.leather.v > 0 && G.res.coin.v < G.res.coin.mx) {
  r.leather -= 0.01 * 5;   // -0.01/s
  r.coin += 0.005 * 5;     // +0.005/s
}
```

#### 灵术消耗折扣

灵狐祠「引灵」的灵术消耗 -20% 修改 `canSpell()` / `castSpell()` 中的费用检查与扣除逻辑。折扣后费用向上取整（`Math.ceil`），与商队造价折扣保持一致：

```
var mul = G.bldSpec.shrine === 'B' ? 0.8 : 1;
for (const p of SD[id].cost)
  if (G.res[p.r].v < Math.ceil(p.a * mul)) return false;
```

示例：祈雨符咒消耗 5 × 0.8 = 4.0 → `Math.ceil(4.0)` = 4；灵狐庇护符咒消耗 8 × 0.8 = 6.4 → `Math.ceil(6.4)` = 7。

#### 上限翻倍类专精交互

储藏窖「深窖」与藏书阁「秘阁」均含"翻倍"效果，需明确作用范围：

- **深窖**：将储藏窖自身在 `calcMx()` 中提供的各资源上限加成翻倍（即 `e[k+'Mx'] * s.c` 变为 `e[k+'Mx'] * s.c * 2`），**仅影响储藏窖自身的上限贡献**。
- **秘阁**：将藏书阁自身的 `loreMx` 贡献翻倍（同理仅乘于藏书阁的 `loreMx * s.c`），**不**翻倍其他建筑（如储藏窖）对学识上限的贡献。

两者同时激活时，储藏窖的 `loreMx` 贡献 ×2，藏书阁的 `loreMx` 贡献 ×2，总学识上限 = 基础 + 储藏窖贡献×2 + 藏书阁贡献×2 + 其他建筑贡献，**不是** 4 倍。

#### 研究费用折扣

学者「通览」的研究费用 -10% 需新增 `researchCostMul()` 函数，返回当前研究费用乘数。同时修改 `canU()` 和 `research()` 中的费用检查与扣除逻辑，应用该乘数。

#### 商队图纸生成

`trySpawnCaravan()` 成功生成商队后，50% 概率（商贩「广路」激活时为 60%）调用 `rollBlueprint(caravanId)`，从该商队图纸池中随机抽一张。排除规则：
- 已在 `G.blueprints` 中持有的
- 已在 `G.bldSpec` / `G.jobTalent` 中激活的
- 同目标另一方向已在 `G.blueprints` 中持有的
- 对应资源上限为 0 的（玩家无法支付）

池中无可选项时不生成。结果存入 `G.caravan.blueprint`：
```
G.caravan = { id: picked, bought: {}, blueprint: { id, target, spec, cost } | null }
```

购买图纸后，执行 `G.caravan.bought['blueprint'] = true`（与普通商品购买一致），将图纸写入 `G.blueprints`，并产生日志："购入图纸：[图纸名]。"

激活专精/天赋时同样产生日志："激活专精：[建筑名]「[专精名]」。" / "激活天赋：[职业名]「[天赋名]」。"

### 9.7 UI 改动

| 位置 | 改动 |
|------|------|
| 山外 Tab 商队面板 | 图纸商品行：显示图纸名 + 目标建筑/职业 + 效果预览 + 价格 + 购买按钮 |
| 工坊 Tab | 新增「图纸」区域（位于制作配方列表下方），列出已持有图纸，每张可展开查看效果描述和激活按钮；无图纸时该区域隐藏；建筑专精图纸在建筑数量 < 5 时激活按钮置灰，显示"需要 [建筑名] ≥5 座" |
| 营火 Tab 建筑行 | 已激活专精的建筑名称后显示专精标记，如"莓果园 (3)「沃土」" |
| 村落 Tab 职业行 | 已激活天赋的职业名称后显示标记 |
| 悬浮面板 | 建筑/职业悬浮面板增加专精效果说明 |

### 9.8 存档兼容

`migrate()` 补齐：
```
G.blueprints = G.blueprints || []
G.bldSpec = G.bldSpec || {}
G.jobTalent = G.jobTalent || {}
```

### 9.9 涉及文件改动

| 文件 | 改动 |
|------|------|
| `data.js` | 新增 SPEC_BD（建筑专精定义，含 `tip` 数组）、SPEC_JD（职业天赋定义，含 `tip` 数组）、CVD 各商队增加 blueprintPool 字段 |
| `engine.js` | `calcR()` 增加专精乘数/附加产出；`calcMx()` 增加储藏窖专精上限；`bp()` 调用 `specCostMul()`；`trySpawnCaravan()` 增加 `rollBlueprint()`；新增 `activateSpec(blueprintId)` / `activateJobTalent(blueprintId)`；`migrate()` 补齐 |
| `ui.js` | 商队面板增加图纸行；工坊 Tab 增加图纸区域；建筑行/职业行增加专精标记；悬浮面板扩展 |
| `style.css` | 图纸商品行样式、专精标记样式、图纸背包区域样式 |
| `docs/design.md` | 本章节 |
| `docs/changelog.md` | 追加 v0.12.0 |
